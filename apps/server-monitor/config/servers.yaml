# Server Monitor — Fly.io Configuration
# Only monitors services reachable from this Fly Machine.
# Update URLs as services are deployed to Fly.

servers:
  # Self-monitoring (always works)
  - name: "Server Monitor"
    type: http
    metrics_endpoint: "http://127.0.0.1:8080/metrics"
    poll_every: 10

  # Uncomment as services are deployed to Fly:
  #
  # - name: "Alities Engine"
  #   type: http
  #   metrics_endpoint: "https://billdonner-alities-engine.fly.dev/metrics"
  #   poll_every: 15
  #
  # - name: "Nagzerver"
  #   type: http
  #   metrics_endpoint: "https://billdonner-nagzerver.fly.dev/api/v1/metrics"
  #   poll_every: 30
  #
  # - name: "Redis"
  #   type: redis
  #   host: "billdonner-redis.internal"
  #   port: 6379
  #   poll_every: 10
  #
  # - name: "Postgres"
  #   type: postgres
  #   dsn: "postgresql://postgres:PASSWORD@billdonner-postgres.internal:5432/nagz"
  #   system_stats: true
  #   poll_every: 15
