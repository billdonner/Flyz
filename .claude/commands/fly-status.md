Show status of all Fly.io apps.

Run ~/Flyz/scripts/status.sh and display results.
Also check each app's public URL with a quick curl to verify it's responding.

Report as a table: app name, fly status, region, machines, HTTP status.
